<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    <div class="main h-screen w-full bg-zinc-900 p-10 text-white">
        <nav class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-2" href="/logout"> Logout</a>
        </nav>
        <h3 class="text-3xl  text-white"><span class="font-light">Hello</span> , <%= user.name %> 👋</h3>
        <h3 class="mb-5"> you can create your new post</h3>
       <form action="/post" method="post">
        <textarea class=" rounded-md p-3 resize-none  bg-transparent outline-none w-1/3 border-2 border-zinc-800" name="postdata" placeholder="what's in your minddd..."></textarea>
        <input type="submit" value="Create post" class="px-3 py-2 w-40 bg-blue-500 text-white rounded-md block mt-2" >
        
       </form>

        <div class="posts mt-20">
            <h2 class=" text-white mb-4">Your Posts</h2>
            <div class="post-conatiner">
             <% user.posts.reverse().forEach(function(post){ %>
                <div class="posts w-1/3 border-2 rounded-md mb-2 border-zinc-700 bg-zinc-800 p-4">
                    <h3 class="mb-2 text-blue-500">@<%= user.username %></h3>
                    <p class="text-sm tracking-tight"> <%= post.postdata %></p>
                    <small class="text-zinc-400"> <%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-3">
                        <a class="text-sm text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                        </a>
                        <a class="text-sm text-zinc-500 " href="/edit/<%= post._id %>">Edit</a>
                    </div>
                </div>
            <% }) %>
              
            </div>
        </div>

    </div>
    
</body>
</html>